import json
import spacy

def run_pipeline(raw_text: str, show_debug: bool = False):
    """
    Will be for the Serie III pdf, presentes no site do JORAM
    
    """

# Normalize + tokenizer-only pipeline
